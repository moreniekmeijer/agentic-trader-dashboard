---
phase: 01-api-foundation-and-safety-boundaries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/index.css
  - src/lib/api/httpClient.ts
  - src/lib/api/contracts.ts
  - src/lib/api/adapters.ts
  - src/lib/api/pollingPolicy.ts
  - src/lib/api/queries.ts
  - src/components/dashboard/PanelFrame.tsx
  - src/components/dashboard/RefreshBar.tsx
  - src/components/dashboard/ContractWarning.tsx
  - src/components/dashboard/LastUpdated.tsx
  - src/types/monitoring.ts
  - package.json
autonomous: true
requirements:
  - SAFE-01
user_setup: []
must_haves:
  truths:
    - "D-01/D-02: API responses are normalized through a dedicated adapter layer before UI consumption."
    - "D-03/D-04: Panel failures are isolated and schema drift is operator-visible with last-good timestamp."
    - "D-05/D-06/D-07: Polling uses centralized per-domain policy, background throttling, and capped exponential backoff."
    - "D-08: UI supports both global and per-panel refresh actions."
    - "D-09/D-10/D-11 + SAFE-01: v1 remains monitor-only with no mutation/control endpoint usage or UI controls."
  artifacts:
    - "src/lib/api/adapters.ts"
    - "src/lib/api/pollingPolicy.ts"
    - "src/components/dashboard/PanelFrame.tsx"
    - "src/components/dashboard/RefreshBar.tsx"
  key_links:
    - "src/lib/api/queries.ts imports and uses polling policy + adapters for all dashboard data hooks."
    - "Dashboard panel components consume normalized domain types from src/types/monitoring.ts (not raw API payloads)."
    - "App shell renders monitor-only dashboard interactions and excludes any trade/strategy control affordances."
---

<objective>
Implement the Phase 1 frontend foundation that turns the starter app into a monitor-only dashboard shell with reliable API boundaries.

Purpose: Lock in the architecture decisions from `01-CONTEXT.md` and `01-UI-SPEC.md` so all later feature phases build on stable contracts.
Output: Typed API boundary, adapter/contract layer, centralized polling policy, panel-level failure UX primitives, and monitor-only shell wiring.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-api-foundation-and-safety-boundaries/01-CONTEXT.md
@.planning/phases/01-api-foundation-and-safety-boundaries/01-UI-SPEC.md
@src/main.tsx
@src/App.tsx
@src/index.css
@src/App.css
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish monitor-only dashboard shell and domain contracts</name>
  <files>src/App.tsx, src/index.css, src/types/monitoring.ts, src/components/dashboard/PanelFrame.tsx, src/components/dashboard/RefreshBar.tsx, src/components/dashboard/LastUpdated.tsx, src/components/dashboard/ContractWarning.tsx</files>
  <read_first>src/App.tsx, src/index.css, .planning/phases/01-api-foundation-and-safety-boundaries/01-CONTEXT.md, .planning/phases/01-api-foundation-and-safety-boundaries/01-UI-SPEC.md</read_first>
  <action>Replace starter Vite content with a monitor-only dashboard shell that includes overview/panel placeholders and refresh affordances aligned with UI-SPEC copy/spacing/typography/color contracts. Introduce explicit domain types in src/types/monitoring.ts for AgentHealth, PositionSnapshot, TradeRecord, and AlertEvent. Build reusable panel primitives that support loading/error/empty/data states and contract-warning rendering. Do not add any strategy/trade control actions or mutation-oriented affordances.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - src/App.tsx contains "Refresh Data" and no control-action verbs like "Start Strategy", "Stop Strategy", or "Execute Trade".
    - src/components/dashboard/PanelFrame.tsx exposes panel-level loading/error/contract-warning handling.
    - src/components/dashboard/ContractWarning.tsx includes copy for data contract issues plus a last-good timestamp slot.
    - src/types/monitoring.ts exports typed monitoring domain models used by dashboard panels.
  </acceptance_criteria>
  <done>Dashboard shell compiles, is monitor-only, and includes reusable panel primitives tied to typed monitoring models.</done>
</task>

<task type="auto">
  <name>Task 2: Build API boundary with validation, adapters, and polling policy</name>
  <files>src/lib/api/httpClient.ts, src/lib/api/contracts.ts, src/lib/api/adapters.ts, src/lib/api/pollingPolicy.ts, src/lib/api/queries.ts, package.json</files>
  <read_first>package.json, .planning/phases/01-api-foundation-and-safety-boundaries/01-CONTEXT.md, .planning/research/STACK.md</read_first>
  <action>Add TanStack Query and zod dependencies, then implement a read-oriented API layer: http client utility, runtime contract schemas, adapter mapping from raw payloads to domain models, and centralized per-domain polling policies. Implement background-tab throttling and capped exponential backoff in polling policy utilities. Wire query hooks to always pass through adapter functions before UI consumption; never expose raw payload objects to panel components.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - package.json includes @tanstack/react-query and zod.
    - src/lib/api/adapters.ts maps raw payload shapes to exported domain models.
    - src/lib/api/pollingPolicy.ts declares per-domain intervals and background-throttle behavior.
    - src/lib/api/queries.ts uses centralized polling policy and adapter functions for all hooks.
  </acceptance_criteria>
  <done>All monitoring data access flows through validated contracts, dedicated adapters, and centralized polling behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce read-only behavior and integrate panel-isolated failure UX</name>
  <files>src/lib/api/queries.ts, src/components/dashboard/PanelFrame.tsx, src/App.tsx, src/components/dashboard/RefreshBar.tsx</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-api-foundation-and-safety-boundaries/01-CONTEXT.md, src/components/dashboard/PanelFrame.tsx, src/App.tsx</read_first>
  <action>Implement query-state wiring so each panel can fail independently while the rest remain usable, including last-success timestamps and schema-drift warning surfaces. Add global refresh and per-panel refresh trigger paths to satisfy D-08 while preserving monitor-only scope. Ensure the app has no mutation endpoint client functions (POST/PUT/PATCH/DELETE trading controls) and no UI controls suggesting write actions; keep all data flows read-only by convention and module design.</action>
  <verify>npm run lint && npm run build</verify>
  <acceptance_criteria>
    - Panel error state does not block rendering of unrelated dashboard panels.
    - UI shows "last updated" metadata per panel and exposes both panel-level and global refresh controls.
    - No mutation/trade-control endpoint functions are introduced in src/lib/api/.
    - SAFE-01 remains satisfied: UI contains monitoring-only interactions.
  </acceptance_criteria>
  <done>Read-only monitor behavior is preserved and resilient panel-level failure handling is fully wired.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run lint passes with newly added modules/components.
- [ ] npm run build passes with typed API/query integration.
- [ ] Code inspection confirms no mutation controls/endpoints added in v1 foundation.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 1 success criteria from ROADMAP are directly addressed by implemented artifacts
</success_criteria>

<output>
After completion, create `.planning/phases/01-api-foundation-and-safety-boundaries/01-01-SUMMARY.md`
</output>
