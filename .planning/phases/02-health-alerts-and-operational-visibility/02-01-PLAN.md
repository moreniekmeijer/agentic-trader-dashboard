---
phase: 02-health-alerts-and-operational-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/monitoring.ts
  - src/lib/api/contracts.ts
  - src/lib/api/adapters.ts
  - src/lib/api/queries.ts
  - src/components/dashboard/PanelFrame.tsx
  - src/components/dashboard/LastUpdated.tsx
  - src/components/dashboard/RefreshBar.tsx
  - src/components/dashboard/HealthPanel.tsx
  - src/components/dashboard/AlertsPanel.tsx
  - src/components/dashboard/LogsPanel.tsx
  - src/App.tsx
  - src/index.css
autonomous: true
requirements:
  - HLTH-01
  - HLTH-02
  - HLTH-03
  - ALRT-01
  - ALRT-02
  - ALRT-03
user_setup: []
must_haves:
  truths:
    - "D-02: Monitor-only scope remains enforced (no mutation/control interactions)."
    - "D-03/D-09: Panel-level failures remain isolated with clear, consistent state grammar."
    - "D-04/D-05: Centralized polling and last-updated visibility remain intact."
    - "D-06: Health view prioritizes down, then degraded, then healthy entities."
    - "D-07: Alerts are ordered by severity first, then recency."
    - "D-08: Logs panel starts compact with minimal operator-friendly filtering."
  artifacts:
    - "src/components/dashboard/HealthPanel.tsx"
    - "src/components/dashboard/AlertsPanel.tsx"
    - "src/components/dashboard/LogsPanel.tsx"
    - "src/lib/api/queries.ts"
  key_links:
    - "Health/alerts/log data is normalized in adapters before UI rendering."
    - "PanelFrame handles empty/error/contract warning states per panel."
    - "App shell composes health, alerts, and logs views without introducing write paths."
---

<objective>
Implement Phase 2 operational visibility surfaces on top of Phase 1 foundation: health status, alerts feed, and recent operational logs.

Purpose: Deliver high-signal operator monitoring for status and incidents while preserving strict monitor-only boundaries.
Output: Typed health/alerts/log query paths, severity/state-aware UI panels, and resilient panel-isolated rendering.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-health-alerts-and-operational-visibility/02-CONTEXT.md
@.planning/phases/02-health-alerts-and-operational-visibility/02-UI-SPEC.md
@.planning/phases/01-api-foundation-and-safety-boundaries/01-CONTEXT.md
@src/App.tsx
@src/index.css
@src/lib/api/contracts.ts
@src/lib/api/adapters.ts
@src/lib/api/queries.ts
@src/components/dashboard/PanelFrame.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend typed monitoring contracts for health, alerts, and logs</name>
  <files>src/types/monitoring.ts, src/lib/api/contracts.ts, src/lib/api/adapters.ts</files>
  <read_first>src/types/monitoring.ts, src/lib/api/contracts.ts, src/lib/api/adapters.ts, .planning/phases/02-health-alerts-and-operational-visibility/02-CONTEXT.md</read_first>
  <action>Add or refine typed models for service/agent health, alert records, and operational log events. Extend runtime validation schemas for corresponding API payloads and ensure adapter mapping produces normalized UI-ready models only. Include deterministic normalization for status and severity values so ordering logic remains stable.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - Domain models exist for health units, alert events, and log entries.
    - Contracts validate expected payload structure and expose contract warnings on drift.
    - Adapters normalize status/severity/timestamp fields into consistent shapes for rendering.
  </acceptance_criteria>
  <done>Health/alerts/log data boundary is typed, validated, and normalized.</done>
</task>

<task type="auto">
  <name>Task 2: Implement health and alerts operational panels</name>
  <files>src/components/dashboard/HealthPanel.tsx, src/components/dashboard/AlertsPanel.tsx, src/lib/api/queries.ts, src/components/dashboard/PanelFrame.tsx, src/components/dashboard/LastUpdated.tsx, src/components/dashboard/RefreshBar.tsx, src/index.css</files>
  <read_first>src/components/dashboard/PanelFrame.tsx, src/components/dashboard/RefreshBar.tsx, src/lib/api/queries.ts, .planning/phases/02-health-alerts-and-operational-visibility/02-UI-SPEC.md</read_first>
  <action>Create dedicated health and alerts panels that reuse panel primitives and preserve panel-level refresh + failure isolation. Implement health ordering (down/degraded/healthy) and alert ordering (critical/warning/info, newest first within severity). Render clear severity/status treatments aligned with UI-SPEC while keeping density operator-friendly.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - Health panel makes degraded/down states immediately visible and prioritized.
    - Alert feed updates through polling and clearly distinguishes severity levels.
    - Last-updated metadata is visible for both panels.
    - Panel-level errors do not block unrelated panels.
  </acceptance_criteria>
  <done>Health and alerts operational surfaces are fully integrated and resilient.</done>
</task>

<task type="auto">
  <name>Task 3: Deliver compact operational logs view and compose Phase 2 dashboard layout</name>
  <files>src/components/dashboard/LogsPanel.tsx, src/lib/api/queries.ts, src/App.tsx, src/index.css</files>
  <read_first>src/App.tsx, src/lib/api/queries.ts, .planning/phases/02-health-alerts-and-operational-visibility/02-UI-SPEC.md</read_first>
  <action>Implement recent operational logs panel with compact rows (timestamp, source, level, message) and minimal filters (source/level/time-window). Compose updated App layout so health, alerts, and logs coexist cleanly with existing monitor panels. Preserve monitor-only boundary and ensure no mutation endpoints or control UI are introduced.</action>
  <verify>npm run lint && npm run build</verify>
  <acceptance_criteria>
    - Logs panel surfaces recent events and supports minimal filtering affordances.
    - UI state grammar for empty/error/degraded/data is consistent with Phase 1 primitives.
    - Global dashboard remains usable during partial endpoint failures.
    - No control-plane/trade mutation pathways are added.
  </acceptance_criteria>
  <done>Phase 2 dashboard composition provides operator-ready health/alert/log visibility.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run lint passes for new/updated panel and API modules.
- [ ] npm run build passes with Phase 2 query/panel integration.
- [ ] Manual UI check confirms ordering rules and panel isolation behavior.
- [ ] Code inspection confirms monitor-only scope is preserved.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No new write/control behavior introduced
- Roadmap Phase 2 success criteria map directly to shipped artifacts
</success_criteria>

<output>
After completion, create `.planning/phases/02-health-alerts-and-operational-visibility/02-01-SUMMARY.md`
</output>
